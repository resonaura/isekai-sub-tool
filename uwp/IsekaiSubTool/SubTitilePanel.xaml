<UserControl
    x:Class="IsekaiSubTool.SubtitlePanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IsekaiSubTool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" x:Name="DlogsWrapper" VerticalAlignment="Top">
            <StackPanel Opacity="0" x:Name="DLogs">

            </StackPanel>
        </ScrollViewer>
        
        <Grid x:Name="BottomPanel" Opacity="0" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Height="50">
            <Grid.Background>
                <SolidColorBrush Color="{ThemeResource ApplicationPageBackgroundThemeBrush}" Opacity=".7"></SolidColorBrush>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="ReturnButton" Click="ReturnButton_Click"  HorizontalAlignment="Stretch" Background="Transparent" VerticalAlignment="Stretch" Height="50" Style="{ThemeResource ButtonRevealStyle}">
                <StackPanel>
                    <SymbolIcon Symbol="Back"></SymbolIcon>
                    <TextBlock>Вернуться назад</TextBlock>
                </StackPanel>
            </Button>
            <Button Grid.Column="1" x:Name="SaveButton" Click="SaveButton_Click" HorizontalAlignment="Stretch" Background="Transparent" VerticalAlignment="Stretch" Height="50" Style="{ThemeResource ButtonRevealStyle}">
                <StackPanel>
                    <SymbolIcon Symbol="Save"></SymbolIcon>
                    <TextBlock>Сохранить изменения на сервер</TextBlock>
                </StackPanel>
            </Button>
            <ToggleButton x:Name="SubButton"  Checked="ToggleButton_Checked" Unchecked="ToggleButton_Unchecked" Grid.Column="2" HorizontalAlignment="Stretch" Background="Transparent" VerticalAlignment="Stretch" Height="50" Style="{ThemeResource ToggleButtonRevealStyle}">
                <StackPanel>
                    <SymbolIcon Symbol="Bullets"></SymbolIcon>
                    <TextBlock>Отдельное окно субтитра</TextBlock>
                </StackPanel>
            </ToggleButton>
        </Grid>
        <ProgressRing Name="MainProgress" Width="64" Height="64"></ProgressRing>
    </Grid>
</UserControl>
