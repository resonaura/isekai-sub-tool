<UserControl x:Class="IsekaiSubTool_WPF.PersListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IsekaiSubTool_WPF"
             xmlns:materialDesignMahApps="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300"
    d:DesignWidth="1000" Height="300" Margin="0,0,10,0" HorizontalAlignment="Stretch">
    <materialDesignMahApps:Card materialDesignMahApps:ShadowAssist.ShadowDepth="Depth2" Margin="10" Height="300" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch">
        <Grid Height="300" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image x:Name="Poster" Stretch="UniformToFill" Source="{Binding ISource}"></Image>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock x:Name="DHeader" Padding="20,0,0,0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="36" Text="{Binding IHeader}"></TextBlock>
                <TextBlock TextWrapping="Wrap" x:Name="DBox" Padding="20,0,0,0" Grid.Row="1" FontSize="16" Text="{Binding IText}"></TextBlock>
            </Grid>
        </Grid>
    </materialDesignMahApps:Card>
</UserControl>
