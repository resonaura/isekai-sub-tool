<UserControl x:Class="IsekaiSubTool_WPF.DialogListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IsekaiSubTool_WPF"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
                 d:DesignHeight="100"
    d:DesignWidth="400" Height="100" Margin="10">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="50"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button Click="Button_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="Auto">
            <materialDesign:PackIcon Kind="PlayArrow" Height="29" Width="23" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Button>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Margin="10,0,0,0" Grid.Row="0" VerticalAlignment="Center" Name="DHeader" Text="{Binding Header}">
            </TextBlock>
            <Grid Grid.Row="1">
                <TextBox VerticalScrollBarVisibility="Auto"   Height="70px" MinHeight="70px" AcceptsReturn="True" TextWrapping="Wrap" VerticalAlignment="Stretch" TextChanged="DBox_TextChanged"  Text="[sub text]" Padding="5,0,0,0" Name="DBox"></TextBox>
            </Grid>
        </Grid>
        <Grid Grid.Column="2" MouseDown="PackIcon_MouseDown">
            <Grid.Background>
                <SolidColorBrush Color="#000" Opacity=".05"></SolidColorBrush>
            </Grid.Background>
            <materialDesign:PackIcon Kind="ContentCopy" Height="29" Width="23" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</UserControl>
